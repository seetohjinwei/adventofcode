#include "definitions.h"
#include "parser.h"
#include <iostream>
#include <vector>

using namespace std;

int main() {
  /*
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  freopen("dayXX.in", "r", stdin);
  */

  return 0;
}
